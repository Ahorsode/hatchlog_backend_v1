# Server
NODE_ENV=development
PORT=3001

# Database (Supabase Postgres — same DB as poultry-pms)
DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres"

# Supabase Auth (validate client JWTs)
SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
SUPABASE_JWT_SECRET="your-supabase-jwt-secret"

# Internal API key for Next.js Server Actions (BFF). Send as X-HatchLog-Api-Key.
HATCHLOG_INTERNAL_API_KEY="change-me-to-a-long-random-secret"

# Redis (BullMQ workers)
REDIS_URL="redis://127.0.0.1:6379"
