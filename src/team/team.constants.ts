/**
 * Default first-login password for workers invited by an owner/manager.
 * Keep in sync with poultry-pms/src/lib/phone-auth.ts (WORKER_PLACEHOLDER_PASSWORD).
 */
export const WORKER_PLACEHOLDER_PASSWORD = '123456';
